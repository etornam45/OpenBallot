import { Router, type Request, type Response } from "express";

const consistuency_router = Router()


consistuency_router.get("/consistuency", (req: Request, res: Response) => {

})

consistuency_router.post("/consistuency", (req: Request, res: Response) => {
    
})

export default consistuency_router